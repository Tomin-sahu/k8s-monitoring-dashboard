Good choice—this is the kind of project that actually starts looking like real DevOps work. I’ll give you a **simple but impressive Kubernetes Monitoring Dashboard (MVP version)** that you can run on Minikube and later extend with Prometheus/Grafana.

We’ll keep it:

* easy to understand
* fully working
* DevOps-ready (Docker + K8s + Jenkins)

---

# 🧠 What we are building

A web dashboard that shows:

✔ Node status
✔ Pod status
✔ CPU (mock for now)
✔ Memory (mock for now)

👉 Important truth:
Without Prometheus, CPU/memory will be **simulated**, but structure will be real DevOps-ready.

---

# 📁 Project Structure

```bash id="proj1"
k8s-monitoring-dashboard/
│
├── app.py
├── requirements.txt
├── templates/
│   └── index.html
├── static/
│   └── style.css
├── Dockerfile
├── K8s/
│   └── deployment.yaml
    └── service.yaml
└── Jenkinsfile
└── README.md
```

---

# 🐍 1. Backend (Flask app - app.py)

```python id="py1"
from flask import Flask, render_template
import random
import subprocess

app = Flask(__name__)

def get_pods():
    try:
        output = subprocess.getoutput("kubectl get pods")
        return output
    except:
        return "Kubernetes not reachable"

def get_nodes():
    try:
        output = subprocess.getoutput("kubectl get nodes")
        return output
    except:
        return "Kubernetes not reachable"

@app.route("/")
def home():
    data = {
        "cpu": random.randint(10, 90),
        "memory": random.randint(20, 80),
        "pods": get_pods(),
        "nodes": get_nodes()
    }
    return render_template("index.html", data=data)

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5000)
```

---

# 📦 2. requirements.txt

```txt id="py2"
flask
```

---

# 🌐 3. Frontend (HTML - templates/index.html)

```html id="html1"
<!DOCTYPE html>
<html>
<head>
    <title>K8s Monitoring Dashboard</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>

<h1>🚀 Kubernetes Monitoring Dashboard</h1>

<div class="grid">

    <div class="card">
        <h2>🔥 CPU Usage</h2>
        <p>{{ data.cpu }}%</p>
    </div>

    <div class="card">
        <h2>🧠 Memory Usage</h2>
        <p>{{ data.memory }}%</p>
    </div>

    <div class="card">
        <h2>📦 Pods</h2>
        <pre>{{ data.pods }}</pre>
    </div>

    <div class="card">
        <h2>🖥 Nodes</h2>
        <pre>{{ data.nodes }}</pre>
    </div>

</div>

</body>
</html>
```

---

# 🎨 4. CSS (static/style.css)

```css id="css1"
body {
    font-family: Arial;
    background: #0f172a;
    color: white;
    text-align: center;
}

h1 {
    margin-top: 20px;
}

.grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    padding: 20px;
}

.card {
    background: #1e293b;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 0 10px black;
}

pre {
    text-align: left;
    font-size: 12px;
}
```

---

# 🐳 5. Dockerfile

```dockerfile id="docker1"
FROM python:3.9-slim

WORKDIR /app

COPY . .

RUN pip install -r requirements.txt

EXPOSE 5000

CMD ["python", "app.py"]
```

---

# ☸ 6. Kubernetes Deployment

## deployment.yaml

```yaml id="k8s1"
apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-dashboard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
      - name: dashboard
        image: tominsahu/k8s-dashboard:latest
        ports:
        - containerPort: 5000
```

---

## service.yaml

```yaml id="k8s2"
apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
spec:
  type: NodePort
  selector:
    app: dashboard
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30009
```

---

# 🚀 7. Jenkinsfile (CI/CD pipeline)

pipeline {
    agent any

    environment {
        IMAGE = "tominsahu/k8s-dashboard"
        TAG = "latest"
    }

    stages {

        stage('Clone Repo') {
            steps {
                git branch: 'main',
                url: 'https://github.com/<your-username>/k8s-monitoring-dashboard.git'
            }
        }

        stage('Build Docker Image') {
            steps {
                bat "docker build -t %IMAGE%:%TAG% ."
            }
        }

        stage('Docker Login') {
            steps {
                withCredentials([usernamePassword(
                    credentialsId: 'docker-cred',
                    usernameVariable: 'USER',
                    passwordVariable: 'PASS'
                )]) {
                    bat "echo %PASS% | docker login -u %USER% --password-stdin"
                }
            }
        }

        stage('Push Image') {
            steps {
                bat "docker push %IMAGE%:%TAG%"
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                bat "kubectl apply -f k8s/deployment.yaml"
                bat "kubectl apply -f k8s/service.yaml"
            }
        }

        stage('Verify Deployment') {
            steps {
                bat "kubectl get pods"
                bat "kubectl get svc"
            }
        }
    }

    post {
        success {
            echo "🚀 Deployment Successful"
        }
        failure {
            echo "❌ Deployment Failed"
        }
    }
}


README.md

# 🚀 Kubernetes Monitoring Dashboard

## 📌 Overview

A DevOps project that monitors Kubernetes cluster status using a web dashboard.

## 🛠 Tech Stack

- Flask (Python)
- Docker
- Kubernetes (Minikube)
- Jenkins CI/CD
- GitHub

## 🚀 Features

- CPU & Memory monitoring (mock/demo)
- Pod status display
- Node health monitoring
- CI/CD automation with Jenkins

## ⚙️ CI/CD Pipeline

GitHub → Jenkins → Docker Build → Docker Push → Kubernetes Deploy

## 📦 Run Locally

docker build -t k8s-dashboard .
docker run -p 5000:5000 k8s-dashboard

## ☸ Deploy on Kubernetes

kubectl apply -f k8s/
minikube service dashboard-service


---

# 🚀 8. Run it locally

### Build Docker:

```bash id="run1"
docker build -t k8s-dashboard .
```

### Run:

```bash id="run2"
docker run -p 5000:5000 k8s-dashboard
```

---

# ☸ 9. Deploy on Minikube

```bash id="run3"
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
```

```bash id="run4"
minikube service dashboard-service
```

---

# 🎯 What makes this IMPRESSIVE

✔ Flask backend
✔ Kubernetes API integration
✔ Docker containerization
✔ CI/CD with Jenkins
✔ NodePort deployment
✔ Live cluster data display

---

# 🔥 Upgrade path (VERY IMPORTANT)

If you want to make this **interview-level strong**, next upgrades are:

### 🚀 Level 2

* Add Prometheus metrics
* Real CPU/memory stats

### 🚀 Level 3

* Grafana dashboard
* Alerting system

### 🚀 Level 4 (PRO)

* AWS EKS deployment
* Ingress + domain name
* TLS HTTPS

---

# 🧠 Final truth

This project is already:
👉 **Beginner → Intermediate DevOps level**

With Prometheus next → **Job-ready DevOps project**

---

If you want, I can next:
👉 Convert this into **GitHub-ready repo**
👉 Or add **real Prometheus + Grafana monitoring (industry level)**
